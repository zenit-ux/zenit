# SafePay — Components Used

Route: `/safepay`
File: `app/safepay/page.tsx`

---

| Component | Notes |
|-----------|-------|
| `NeuralNoise` | Gold, opacity ~0.32 |
| `EscrowFlowMockup` | Inline — 4 milestones, $80k project |
| `MultiCurrencyMockup` | Inline — USD → BRL conversion example |
| Lucide: `Shield` | Hero section |
| Lucide: `CheckCircle2` | Feature bullet rows |

**Note:** `SafePaySection.tsx` exists in `components/sections/` but is NOT used — content is inline in page.
