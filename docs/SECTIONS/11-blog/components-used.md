# Blog — Components Used

Route: `/blog`
File: `app/blog/page.tsx`

---

| Component | Notes |
|-----------|-------|
| Newsletter form | Inline — fully accessible (`aria-label`, `sr-only` label, `autoComplete`) |
| Post cards | Inline static data array — no dedicated component |
| `NeuralNoise` | Not confirmed in blog, may not be used |

**Missing:** No `/blog/[slug]` dynamic route — blog detail pages need to be created.
